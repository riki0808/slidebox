CONEの皆様
slideboxからの依頼がきましたぞ

==============================================

依頼者名：<%= @doc.user.name %>

会社名：<%= @doc.user.company %>

依頼者メールアドレス：<%= @doc.user.email %>

Google Slide：<%= @doc.g_slide %>

資料ジャンル：<%= @doc.title %>

依頼枚数：<%= @doc.sheet %>

希望納期：<%= @doc.deadline %>

資料の内容・概要：<%= @doc.content %>

資料の活用場面：<%= @doc.scene %>

資料を見せる相手：<%= @doc.shower %>

重複している内容の削除や調整をしてもよろしいですか？：<%= @doc.deplication %>

サイズ：<%= @doc.size %>

納品形式：<%= @doc.format %>

フォント：<%= @doc.font %>

メインカラー：<%= @doc.main_color %>

アクセントカラー：<%= @doc.sub_color %>

依頼内容詳細：<%= @doc.detail %>

==============================================
